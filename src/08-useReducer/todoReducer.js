
export const todoReducer = (initialState, action) => {
  
    switch (action.type) {
        case 'ABC' :
            throw new Error('Tipo de acción ABC no está implementada');
        default :
            break;
    }      
}
